package com.sportstechbrands.sportstechlive.ui.theme

import androidx.compose.ui.graphics.Color

// ── Deep Space Background ──────────────────────────────────────────────────
val SpaceBlack      = Color(0xFF060914)
val SpaceNavy       = Color(0xFF0A0F1E)
val SpaceDark       = Color(0xFF0D1228)
val SpaceMid        = Color(0xFF111830)

// ── Liquid Glass Surfaces ──────────────────────────────────────────────────
val GlassSurface    = Color(0x14FFFFFF)   // 8 % white
val GlassSurfaceMid = Color(0x1FFFFFFF)   // 12 % white
val GlassSurfaceHi  = Color(0x26FFFFFF)   // 15 % white
val GlassBorderTop  = Color(0x52FFFFFF)   // 32 % white  (specular highlight edge)
val GlassBorderBot  = Color(0x14FFFFFF)   // 8  % white  (shadow edge)
val GlassInnerGlow  = Color(0x33FFFFFF)   // 20 % white

// ── Accent Palette ─────────────────────────────────────────────────────────
val AccentCyan      = Color(0xFF00D4FF)   // electric cyan
val AccentCyanDim   = Color(0xFF0099CC)
val AccentGreen     = Color(0xFF00FF88)   // neon green
val AccentPurple    = Color(0xFF7B2FBE)   // deep violet
val AccentPink      = Color(0xFFFF3CAC)   // hot pink
val AccentOrange    = Color(0xFFFF6B35)   // fire orange

// ── Gradient stop helpers ──────────────────────────────────────────────────
val GradientStart   = Color(0xFF00D4FF)
val GradientMid     = Color(0xFF7B2FBE)
val GradientEnd     = Color(0xFFFF3CAC)

// ── Text ───────────────────────────────────────────────────────────────────
val TextPrimary     = Color(0xFFFFFFFF)
val TextSecondary   = Color(0xB3FFFFFF)   // 70 %
val TextTertiary    = Color(0x80FFFFFF)   // 50 %
val TextDisabled    = Color(0x4DFFFFFF)   // 30 %

// ── Semantic ───────────────────────────────────────────────────────────────
val SuccessGreen    = Color(0xFF00FF88)
val WarningAmber    = Color(0xFFFFB830)
val ErrorRed        = Color(0xFFFF4757)
val LiveRed         = Color(0xFFFF3B3B)
